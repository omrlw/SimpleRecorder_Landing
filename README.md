## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set `NEXT_PUBLIC_SITE_URL` in [.env.local](.env.local) to your production URL
3. Run the app:
   `npm run dev`
